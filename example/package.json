{
	"scripts": {
		"dev": "npm run build:untouched && npm run serve",
		"build": "npm run build:untouched && npm run build:optimized",
		"serve": "serve ../",
		"build:untouched": "asc assembly/index.ts --target debug --exportRuntime --exportTable",
		"build:optimized": "asc assembly/index.ts --target release --exportRuntime --exportTable",
		"test": "node tests"
	},
	"dependencies": {
		"@assemblyscript/loader": "^0.18.32",
		"asdom": "^0.1.7"
	},
	"devDependencies": {
		"assemblyscript": "^0.18.11",
		"serve": "^12.0.0"
	}
}
