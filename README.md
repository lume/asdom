# ASWebGLue (Assemblyscript WebGL Bindings and Glue)

Compile render_lines.asc demo using the following:
```
asc render_lines.asc --extension asc --runtime stub --importMemory -o render_lines.wasm
```